# Context Compactor Optimization for Portfolio Agent

## Objective
Optimize the `compact_history` function in `initial_program.py` for the Portfolio Agent.

## Requirements
1. Maintain history within the requested `limit`.
2. Always retain the initial conversation turn (system/user opening intent).
3. Always retain the most recent conversation turns for context continuity.
4. Maximize score in evaluation benchmarks.

## Code Constraints
Modify only code within the `# EVOLVE-BLOCK-START` and `# EVOLVE-BLOCK-END` markers.
