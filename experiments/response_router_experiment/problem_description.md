# Response Router & Intent Classification Optimization

## Objective
Optimize the `route_and_enrich_intent` function to maximize format mapping accuracy and keyword signal extraction for the Portfolio Agent.

## Requirements
1. Correctly map user intent to A2UI formats (`awards`, `certs`, `timeline`, `blog_cards`, `speaker`, `flashcards`, etc.).
2. Extract domain keywords accurately for downstream Vertex AI / Agent Engine context retrieval.
3. Optimize execution speed and rule accuracy.

## Code Constraints
Modify only code within `# EVOLVE-BLOCK-START` and `# EVOLVE-BLOCK-END`.
